export interface BoardProfileModel {
  id: string;
  name: string;
  email: string;
  imageUrl: string;
  profileImageUrl: string;
  profileBio: string;
  description?: string;
  linkedinUrl?: string;
  githubUrl?: string;
  facebookUrl?: string;
}
