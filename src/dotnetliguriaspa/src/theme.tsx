import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#648B2D",
            light: "sky",
        },
        secondary: {
            main: "#15c630"
        },
    },
});

export default theme;